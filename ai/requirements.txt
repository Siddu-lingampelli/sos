opencv-python==5.0.0.93
numpy==2.5.3
torch==2.14.0
torchvision==0.29.0
ultralytics==8.4.155
# ByteTrack: provided built-in via ultralytics (model.track ..., tracker="bytetrack.yaml")
# --- Level 6 audio ---
sounddevice==0.5.6
faster-whisper==1.2.1
# Silero VAD: no pin — torch.hub.load('snakers4/silero-vad') on first use
