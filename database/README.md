# Database — Level 1
- Dev Postgres via `docker compose up postgres` (db `silentsos`, user `silentsos`).
- `init.sql` is a connectivity placeholder. Real tables land in Level 2.
- Backend connection test lands in Level 2 (`/api/health` DB check).
